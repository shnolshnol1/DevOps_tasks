# Создаем директорию
mkdir MyDirectory

# Переходим в созданную директорию
cd MyDirectory

# Создаем файл
touch MyFile.txt

# Возвращаемся в предыдущий каталог
cd ..

# Выводим список файлов и директорий в текущем каталоге
ls -la
